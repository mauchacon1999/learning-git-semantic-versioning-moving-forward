#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔄 Post-merge hook ejecutándose..."

# Obtener la rama actual
CURRENT_BRANCH=$(git branch --show-current)

# Solo crear tags en development y master
if [ "$CURRENT_BRANCH" = "development" ] || [ "$CURRENT_BRANCH" = "master" ]; then
    echo "📦 Rama: $CURRENT_BRANCH - Verificando si crear tag automático..."
    
    # Contar commits desde el último tag
    COMMITS_SINCE_LAST_TAG=$(git rev-list --count $(git describe --tags --abbrev=0 2>/dev/null || git rev-list --max-parents=0 HEAD)..HEAD)
    
    if [ "$COMMITS_SINCE_LAST_TAG" -gt 0 ]; then
        echo "🎯 Encontrados $COMMITS_SINCE_LAST_TAG commits nuevos"
        
        # Determinar tipo de versión basado en la rama y commits
        if [ "$CURRENT_BRANCH" = "master" ]; then
            VERSION_TYPE="major"
        else
            # En development, verificar si hay features o solo fixes
            FEATURE_COMMITS=$(git log --oneline --since="1 day ago" | grep -c "feat:" || echo "0")
            if [ "$FEATURE_COMMITS" -gt 0 ]; then
                VERSION_TYPE="minor"
            else
                VERSION_TYPE="patch"
            fi
        fi
        
        echo "🏷️  Creando tag automático: $VERSION_TYPE"
        powershell -ExecutionPolicy Bypass -File "scripts/git-tag-automation-simple.ps1" -VersionType $VERSION_TYPE -AutoConfirm
    else
        echo "✅ No hay commits nuevos para taggear"
    fi
else
    echo "⏭️  Rama $CURRENT_BRANCH - No se crean tags automáticos"
fi
